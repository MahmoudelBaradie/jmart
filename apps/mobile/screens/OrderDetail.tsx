// This is a re-export wrapper for the Expo Router page at app/order/[id].tsx
// Use that file directly via Expo Router navigation: router.push(`/order/${id}`)
export { default } from '@/app/order/[id]';
