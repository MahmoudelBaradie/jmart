// This is a re-export wrapper for the Expo Router page at app/contract/[id].tsx
// Use that file directly via Expo Router navigation: router.push(`/contract/${id}`)
export { default } from '@/app/contract/[id]';
